doctype html
html
  head
    block head
      meta(charset='UTF-8')
      meta(name='viewport' content='width=device-width, initial-scale=1.0')
      link(rel='stylesheet' href='/css/style.css')
      link(rel='shortcut icon' type='/img/png' href='/img/favicon.png')
      link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
      //- The base template still has access to locals (the variables we passed into the template)
      title Natours | #{title}

  body(data-alert=`${alert ? alert : ''}`)
    // HEADER
    include _header
    
    // CONTENT
    block content 
      h1 This is a placeholder heading

    // FOOTER
    include _footer

    script(type= 'module', src='/js/bundle/index.js')
    script(src='https://js.stripe.com/v3/')
    //- script(src='https://cdn.jsdelivr.net/npm/moment@2.29.4/moment.min.js')
    //- script(src='https://cdn.jsdelivr.net/npm/moment-timezone@0.5.38/moment-timezone-with-data.min.js')
